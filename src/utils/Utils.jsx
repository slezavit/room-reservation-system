export const days = new Array(7).fill(null).map((_, i) => i + 1);

export const times = new Array(15).fill(null).map((_, i) => i + 8);

export const gridPosition = {
  labels: 1,
  allDay: 1,
  columnStart: 1,
  rowStart: 1,
};
